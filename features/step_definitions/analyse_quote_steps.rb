# frozen_string_literal: true

Given(/^the database contains the following quote : (.*)$/) do |_input|
  pending
end

When(/^the analyse is run$/) do
  pending
end

Then(/^the printed output should be (.*)$/) do |_output|
  pending
end
