Given(/^the following quote (.*)$/) do |input|
  pending
end

When(/^a quote is trying to be inserted in the database$/) do
  pending
end

Then(/^the quote should appear only one time in the database$/) do
  pending
end

But(/^the quote is already in the database$/) do
  pending
end